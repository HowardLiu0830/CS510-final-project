name: cs510
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - langchain>=0.3
      - langchain-openai
      - langgraph>=0.2
      - litellm>=1.50
      - openai
      - streamlit
      - streamlit-agraph
      - pyvis
      - networkx
      - pyalex
      - arxiv
      - requests
      - httpx
      - tenacity
      - pydantic>=2
      - pydantic-settings
      - python-dotenv
      - pandas
      - numpy
      - tqdm
      - pytest
      - pytest-asyncio
      - ruff
      - black
      - ipykernel
      - jupyter
